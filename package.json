{
  "name": "tacet",
  "version": "0.1.0",
  "private": true,
  "description": "Tacet — sealed coordination protocol for autonomous agents on Arbitrum",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:contracts": "cd contracts && forge test -vv",
    "e2e:local": "tsx scripts/e2e-local.ts",
    "e2e:agents": "tsx scripts/e2e-agents.ts",
    "deploy:sepolia": "tsx scripts/deploy-sepolia.ts",
    "continue:sepolia": "tsx scripts/continue-sepolia.ts",
    "prove:zerodev": "tsx scripts/prove-zerodev.ts",
    "sync:dune": "tsx scripts/sync-dune.ts",
    "verify:integrations": "tsx scripts/verify-integrations.ts",
    "sync:web": "cp assets/brand/tacet.png apps/web/public/tacet.png && cp outputs/deployment.json apps/web/public/deployment.json 2>/dev/null; cp outputs/sepolia-evidence.json apps/web/public/sepolia-evidence.json 2>/dev/null; cp outputs/sponsor-evidence.json apps/web/public/sponsor-evidence.json 2>/dev/null || true",
    "smoke:sepolia": "tsx scripts/smoke-sepolia.ts"
  },
  "devDependencies": {
    "@duneanalytics/client-sdk": "0.4.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
