{
  "name": "@tacet/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tacet Arbitrum client — viem helpers for sealed coordination rounds",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@tacet/tlock": "workspace:*",
    "viem": "^2.31.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
