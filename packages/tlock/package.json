{
  "name": "@tacet/tlock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drand timelock sealing and commitment encoding for Tacet on Arbitrum",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/*.test.ts",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@noble/ciphers": "^1.2.1",
    "@noble/curves": "^1.9.2",
    "@noble/hashes": "^1.8.0",
    "drand-client": "^1.4.2",
    "tlock-js": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
